/*
function App() {
    const [count, setCount] = useState(1)
    return <Counter 
        count = {count}
        setCount = {setCount}
    />
}

function Counter(props) {
    return<div style = {{fontSize:250}}
    onClick = {() => props.setCount(props.count+1)}>
        {props.count}
    </div>
}
*/